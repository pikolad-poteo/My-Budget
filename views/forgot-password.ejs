<% /* Password reset request page that starts the email token flow. */ %>
<%- include('partials/header') %>

<section class="container auth-section">
  <div class="auth-card">
    <p class="eyebrow"><%= t('auth.accountRecovery') %></p>
    <h1 class="page-title"><%= t('auth.forgotPasswordTitle') %></h1>
    <p class="page-text"><%= t('auth.forgotPasswordText') %></p>

    <% if (errorMessage) { %>
      <div class="auth-message auth-message-error"><%= errorMessage %></div>
    <% } %>

    <% if (successMessage) { %>
      <div class="auth-message auth-message-success"><%= successMessage %></div>
    <% } %>

    <form class="auth-form" action="/forgot-password" method="post">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <div class="form-group">
        <label class="form-label" for="email"><%= t('auth.email') %></label>
        <input
          class="form-input"
          type="email"
          id="email"
          name="email"
          value="<%= email %>"
          placeholder="<%= t('auth.emailPlaceholder') %>"
        />
      </div>

      <button class="btn btn-primary" type="submit"><%= t('auth.sendResetLink') %></button>
    </form>

    <div class="auth-footer">
      <p class="auth-footer-text">
        <%= t('auth.rememberedPassword') %>
        <a href="/login" class="auth-link"><%= t('auth.backToLogin') %></a>
      </p>
    </div>
  </div>
</section>

<%- include('partials/footer') %>
