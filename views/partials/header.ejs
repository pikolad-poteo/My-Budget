<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title ? `${title} | My Budget` : 'My Budget' %></title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <header class="site-header">
    <div class="container header-inner">
      <a href="/dashboard" class="logo">My Budget</a>

      <nav class="nav">
        <a href="/dashboard" class="nav-link <%= activePage === 'dashboard' ? 'is-active' : '' %>">Dashboard</a>
        <a href="/login" class="nav-link <%= activePage === 'login' ? 'is-active' : '' %>">Login</a>
        <a href="/register" class="nav-link <%= activePage === 'register' ? 'is-active' : '' %>">Register</a>
      </nav>
    </div>
  </header>

  <main class="page">