<% /* Wishlist cards partial used for filtered item lists and folder views. */ %>
<section class="row g-4 wishlist-cards-grid" id="wishlistItemsGrid">
  <% if (wishlistItems.length === 0) { %>
    <div class="col-12">
      <article class="card border-0 shadow-sm rounded-4 section-card wishlist-empty-card">
        <div class="card-body p-5 text-center wishlist-empty-content">
          <div class="wishlist-empty-icon"><i class="bi bi-bag-heart"></i></div>
          <h3 class="h4 fw-bold mb-2"><%= hasActiveFilters ? t('wishlist.noItemsFound') : t('wishlist.noWishlistItemsYet') %></h3>
          <p class="wishlist-toolbar-text mb-4">
            <%= hasActiveFilters ? t('wishlist.changeFiltersOrAdd') : t('wishlist.createFirstPlannedPurchase') %>
          </p>

          <button type="button" class="wishlist-empty-add-button" data-wishlist-open-add-choice>
            <span class="wishlist-add-card-icon"><i class="bi bi-plus-lg"></i></span>
            <strong><%= t('wishlist.addItem') %></strong>
            <small><%= t('wishlist.createNewOrAddExisting') %></small>
          </button>
        </div>
      </article>
    </div>
  <% } %>

  <% wishlistItems.forEach((item) => { %>
    <div class="col-sm-6 col-xl-4">
      <a class="card border-0 shadow-sm rounded-4 section-card wishlist-item-card h-100" href="/wishlist/<%= item.id %>">
        <div class="wishlist-item-media">
          <% if (item.image_url) { %>
            <img src="<%= item.image_url %>" alt="<%= item.title %>" />
          <% } else { %>
            <div class="wishlist-image-placeholder"><i class="bi bi-bag-heart"></i><span><%= t('wishlist.preview') %></span></div>
          <% } %>
          <span class="wishlist-card-status is-<%= item.status %>"><%= statusLabels[item.status] || item.status %></span>
        </div>

        <div class="wishlist-item-compact-footer">
          <h3 title="<%= item.title %>"><%= item.title %></h3>
          <strong><%= formatMoney(item.amount) %></strong>
        </div>
        <% if (item.buyer_name) { %>
          <p class="wishlist-item-buyer"><%= t('wishlist.wantedBy') %> <%= item.buyer_name %></p>
        <% } %>
      </a>
    </div>
  <% }) %>

  <% if (wishlistItems.length > 0) { %>
    <div class="col-sm-6 col-xl-4">
      <button type="button" class="card border-0 shadow-sm rounded-4 section-card wishlist-add-card w-100 h-100" data-wishlist-open-add-choice>
        <span class="wishlist-add-card-icon"><i class="bi bi-plus-lg"></i></span>
        <strong><%= t('wishlist.addItem') %></strong>
        <small><%= t('wishlist.createNewOrAddExisting') %></small>
      </button>
    </div>
  <% } %>
</section>
